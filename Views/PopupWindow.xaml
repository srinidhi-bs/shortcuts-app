<?xml version="1.0" encoding="UTF-8" ?>
<winuiex:WindowEx
    x:Class="ShortcutsApp.Views.PopupWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winuiex="using:WinUIEx">

    <Border x:Name="MainBorder" 
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        
        <!-- Main content area -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header with search box (for future implementation) -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Select Application" 
                          FontSize="16" 
                          FontWeight="SemiBold"
                          HorizontalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </Grid>
            
            <!-- Shortcuts grid container -->
            <ScrollViewer Grid.Row="1" 
                         x:Name="ShortcutsScrollViewer"
                         HorizontalScrollMode="Disabled"
                         VerticalScrollMode="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         VerticalScrollBarVisibility="Auto"
                         ZoomMode="Disabled">
                
                <!-- Grid view for shortcuts with keyboard navigation -->
                <GridView x:Name="ShortcutsGridView"
                         SelectionMode="Single"
                         IsItemClickEnabled="True"
                         ItemClick="ShortcutsGridView_ItemClick"
                         SelectionChanged="ShortcutsGridView_SelectionChanged"
                         KeyDown="ShortcutsGridView_KeyDown"
                         GotFocus="ShortcutsGridView_GotFocus"
                         Padding="8">
                    
                    <!-- Custom item template for shortcuts -->
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ShortcutItemBorder"
                                   Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                   BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                   BorderThickness="1"
                                   CornerRadius="6"
                                   Width="80"
                                   Height="80"
                                   Padding="8">
                                
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Icon placeholder - will be replaced with actual icons -->
                                    <Border Grid.Row="0" 
                                           Background="{ThemeResource AccentFillColorDefaultBrush}"
                                           CornerRadius="4"
                                           Width="32"
                                           Height="32"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center">
                                        
                                        <FontIcon Glyph="&#xE8B7;" 
                                                 FontSize="16"
                                                 Foreground="White"/>
                                    </Border>
                                    
                                    <!-- Application name -->
                                    <TextBlock Grid.Row="1"
                                              Text="{Binding Name}"
                                              FontSize="10"
                                              TextAlignment="Center"
                                              TextWrapping="Wrap"
                                              MaxLines="2"
                                              Margin="0,4,0,0"
                                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                    
                    <!-- Custom item container style for hover and selection effects -->
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="Margin" Value="4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="GridViewItem">
                                        <Border x:Name="ContentBorder"
                                               Background="Transparent"
                                               CornerRadius="8">
                                            
                                            <!-- Visual states for selection and hover -->
                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal"/>
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="ContentBorder.Background" 
                                                                   Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="ContentBorder.Background" 
                                                                   Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                                
                                                <VisualStateGroup x:Name="SelectionStates">
                                                    <VisualState x:Name="Unselected"/>
                                                    <VisualState x:Name="Selected">
                                                        <VisualState.Setters>
                                                            <Setter Target="ContentBorder.Background" 
                                                                   Value="{ThemeResource AccentFillColorSecondaryBrush}"/>
                                                            <Setter Target="ContentBorder.BorderBrush" 
                                                                   Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                                            <Setter Target="ContentBorder.BorderThickness" 
                                                                   Value="2"/>
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>
                                            
                                            <ContentPresenter Content="{TemplateBinding Content}"
                                                             ContentTemplate="{TemplateBinding ContentTemplate}"
                                                             HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                             VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>
            </ScrollViewer>
            
            <!-- Empty state when no shortcuts configured -->
            <StackPanel Grid.Row="1" 
                       x:Name="EmptyStatePanel"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed">
                
                <FontIcon Glyph="&#xE8B7;" 
                         FontSize="48"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,16"/>
                
                <TextBlock Text="No shortcuts configured"
                          FontSize="16"
                          HorizontalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                          Margin="0,0,0,8"/>
                
                <TextBlock Text="Add shortcuts in the settings window"
                          FontSize="14"
                          HorizontalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</winuiex:WindowEx>